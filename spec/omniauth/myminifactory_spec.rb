# frozen_string_literal: true

RSpec.describe Omniauth::Myminifactory do
  it "has a version number" do
    expect(Omniauth::Myminifactory::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(Omniauth::Myminifactory).not_to be_nil
  end
end
